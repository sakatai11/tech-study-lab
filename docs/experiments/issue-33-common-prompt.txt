tech-study-lab の Issue #33 を実装してください。あなたはこの実行のメインエージェント Sol xhigh です。最後まで実装と必要な品質確認を進めてください。

比較実験のための共通条件（プロジェクト手順より優先）:
- このcwdの開始状態のみ参照してください。他worktree・別branch・過去タスク・GitHub comments/PR/旧実装は閲覧禁止です。git logの履歴調査、web/GitHub調査は行わないでください。必要な仕様は以下と現在の設計書にあります。
- AGENTS.md、.ai/agents/developer.md、.ai/runtime-compatibility.md と関連 .claude/rules を読み、設計先行・選択的TDD・品質ガードを守ってください。
- モデルと委譲を統一するため、この実行ではあなた自身が調査・設計・実装・修正を担当しサブエージェントを起動しないでください。独立レビューとコミット・PRは親が後で担当します。自己レビューを独立レビュー済みとはしないこと。
- git commit/push/branch切替、外部書込み、環境/認証設定変更はしないでください。.env/.env.*、.dev.vars、認証情報・秘密を含むファイルは読み書きしないでください。
- API/shared契約やdesign.mdの必要な変更を許可します。新たな機能範囲を追加せず、Issue本文の曖昧さは既存設計と最小構成から判断し仕様に記録してください。
- 依存は用意済みです。pnpm typecheck / pnpm lint / pnpm test / pnpm build を実行し、失敗は既存か変更起因か区別して修正してください。sandboxが阻むものはコマンドとエラーを報告し、残る実装を進めてください。
- 最終報告に受け入れ条件ごとの実装箇所、テストとコマンド結果、未確認事項、仕様判断を簡潔に記載してください。

Issue #33: [Feature] dashboard: GET /activity/recent とダッシュボード完成（統計・領域別・アクティビティ）

目的: ダッシュボードを最小版（due件数のみ）から、学習統計・領域別習得状況・最近のアクティビティ・次のレッスン導線へ拡張する。

受け入れ条件:
1. GET /activity/recent: レッスン閲覧・演習完了・復習キュー更新を統合した直近イベント一覧を返す。集計ロジックは簡易版でよい。
2. recentActivityResponseSchemaをpackages/sharedのschema/api.tsへ追加する。
3. route→service→deps（DAL実装）の既存パターンで実装する。
4. ダッシュボードに学習統計（正答率・学習時間・連続学習日数）、領域別習得状況、最近のアクティビティ、次のレッスン導線を実装する。
5. GET /activity/recent のIF（URL、HTTP status、JSON、error）を確定し、共有Zodとdocs/api-spec.htmlを同時更新する。

既存analytics/summaryとdomainsのAPI・部品を利用する。設計該当節は §7.1/7.3/10.1 と関連章。開始時design.mdにはdashboard統計が静的、領域別は/domains専任と書かれているが、このIssueはdashboardへの追加を要求するため、該当箇所を実装より先に改訂する。既存/domainsは維持する。PoCや実験機構の実装はこの実行の担当外。

